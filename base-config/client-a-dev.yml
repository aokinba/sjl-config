server:
  port: 7070
spring:
  application:
    name: client-a
eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:127.0.0.1}:${eureka.port:8761}/eureka/
  instance:
    prefer-ip-address: true
    
cn: 
 springcloud: 
          book: 
            config: I am the git configuration file from dev environment. I will edit it. 111111111111