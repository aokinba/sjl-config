server:
  port: 7070
spring:
  application:
    name: client-a
  ## 配置rabbitMQ 信息
  rabbitmq:
      host: localhost
      port: 5672
      username: guest
      password: guest
  cloud:
      bus:
        trace:
            enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:127.0.0.1}:${eureka.port:8761}/eureka/
  instance:
    prefer-ip-address: true
    
cn: 
 springcloud: 
          book: 
            config: I am the git configuration file from dev environment. I will edit it. 2222222222222